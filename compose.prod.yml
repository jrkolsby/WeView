version: "3"
services:
  ch-server:
    build: 
      context: ./wv-server
      dockerfile: Dockerfile-prod
    ports:
      - 5000:5000
  ch-client:
    build: 
      context: ./wv-client
      dockerfile: Dockerfile-prod
    ports:
      - 80:80
